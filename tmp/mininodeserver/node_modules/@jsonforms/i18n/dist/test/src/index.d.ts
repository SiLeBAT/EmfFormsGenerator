import { Labels } from '@jsonforms/core';
import { i18nReducer } from './reducers';
export interface Translations {
    [key: string]: string;
}
/**
 * Number is formatted based on the current locale value.
 * If user enters a decimal point, the corresponding decimal separator is added as the last
 * character of the locale string
 */
export declare const fromNumber: (locale: string) => (data: number) => string;
/**
 * It coverts the given string number by the user to a valid string number before updating state
 * Local thousands separators are removed, local decimal separators are replaced with '.'
 */
export declare const toNumber: (value: string) => number;
export declare const translateLabel: (translations: Translations, label: string | Labels) => string | Labels;
export declare const translateProps: (state: any, props: any) => any;
export { i18nReducer };
