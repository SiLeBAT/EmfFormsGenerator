/// <reference types="react" />
import { JsonSchema, UISchemaElement } from '@jsonforms/core';
export interface MaterialLayoutRendererProps {
    elements: UISchemaElement[];
    schema: JsonSchema;
    path: string;
    visible: boolean;
    direction: 'row' | 'column';
}
export declare const MaterialLayoutRenderer: ({visible, elements, schema, path, direction}: MaterialLayoutRendererProps) => JSX.Element;
