import 'jsdom-global/register';
import { JsonFormsStore, JsonSchema, UISchemaElement } from '@jsonforms/core';
/**
 * Describes the initial state of the JSON Form's store.
 */
export interface JsonFormsInitialState {
    /**
     * Data instance to be rendered.
     */
    data: any;
    /**
     * JSON Schema describing the data to be rendered.
     */
    schema?: JsonSchema;
    /**
     * UI Schema describing the UI to be rendered.
     */
    uischema?: UISchemaElement;
    /**
     * Any additional state.
     */
    [x: string]: any;
}
export declare const initJsonFormsStore: ({data, schema, uischema, ...props}: JsonFormsInitialState) => JsonFormsStore;
